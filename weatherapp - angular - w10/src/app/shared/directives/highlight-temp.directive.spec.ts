import { HighlightTempDirective } from './highlight-temp.directive';

describe('HighlightTempDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightTempDirective();
    expect(directive).toBeTruthy();
  });
});
